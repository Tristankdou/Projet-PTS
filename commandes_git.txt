Commandes GitHub

1) git add nom_fichier
    ou add * (ajoute tous les fichiers)
2) git commit -m "commentaire sur les modifications"
3) git push

Commandes terminal : (pour importer une classe)
ls affiche où tu est
cd dossier


# Paramètres de la fenêtre
WIDTH, HEIGHT = 1200, 700
screen = pygame.display.set_mode((WIDTH, HEIGHT))
pygame.display.set_caption("Cat Wars")

# Boucle principale du jeu
running = True
while running:
    for event in pygame.event.get():
        if event.type == pygame.QUIT:
            running = False

    # Remplir l'écran en blanc
    screen.fill((255, 255, 255))

    # Rafraîchir l'écran
    pygame.display.flip()

# Quitter Pygame
pygame.quit()